{
  "name": "demo-control-panel",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.9.0"
  },
  "scripts": {
    "demo": "tsx --env-file-if-exists=.env demo.ts",
    "panel": "tsx --env-file-if-exists=.env server.ts",
    "install-browser": "playwright install chromium",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "playwright": "^1.49.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
